<footer class="pt-4 border-top text-center">
            <small class="text-muted">DaviA © 2024</small>
        </footer>

        <div class="notifications"></div>

        <script src="/js/app.js"></script>
        <script defer>
          const notification = <%- JSON.stringify(notification || null) %>;
          if (notification !== null) {
              console.log('Notification trouvée:', notification.message, notification.level);
              new CustomNotification(notification.message, notification.level, 5000);
          } else {
              console.log('Aucune notification disponible.');
          }
      </script>
    <script src="/js/websocket.js" defer></script>
    </body>
</html> 