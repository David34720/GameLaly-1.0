<%- include('partials/head') %>

        <main class="container d-flex flex-column py-4">
            <!-- Welcome -->
            <div class="text-center">
                <h2 class="h1 my-4">Bienvenue sur Laly's Game</h2>
                <p class="lead">
                    Ce jeu a été créé par Laly 10 ans, l'interface du jeu a été codé par son papa dans le cadre de ses études en développement web.
                    Histoire du jeu :
                </p>
            </div>

            <!-- Cards -->
            
            <div class="row mb-3 text-center flew-wrap">
                <% for (let i = 0; i < maps.length; i++) { %>
                    <% let map = maps[i] %>
                    <div class="col-4">
                        <div class="card mb-4">
                            <div class="card-header">
                                <h3 class="h2"> <%= map.name %> </h3>
                            </div>
                            <div class="card-body d-flex flex-column justify-content-between">
                                <h4 class="h3"> <%= map.description %></h4>
                                <p>par <%= map.user %></p>
                                <ul class="list-unstyled mb-4 p-0">
                                    <li class="badge bg-primary">Débutant</li>
                                   
                                </ul>
                                <a href="#"
                                     class="btn btn-lg btn-primary text-white"
                                    >Jouer</a>
                            </div>
                        </div>
                    </div>
                <% }; %>
               
            </div>
        </main>



<%- include('partials/foot') %>